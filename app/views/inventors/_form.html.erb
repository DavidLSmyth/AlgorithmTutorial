<%= form_for(@inventor) do |f| %>
  <% if @inventor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inventor.errors.count, "error") %> prohibited this inventor from being saved:</h2>

      <ul>
      <% @inventor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :about %><br>
    <%= f.text_field :about %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
