<%= form_for(@relation) do |f| %>
  <% if @relation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@relation.errors.count, "error") %> prohibited this relation from being saved:</h2>

      <ul>
      <% @relation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :algorithms_id %><br>
    <%= f.text_field :algorithms_id %>
  </div>
  <div class="field">
    <%= f.label :inventor %><br>
    <%= f.text_field :inventor %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
